# v2026.422.0

> Released: 2026-04-22

## Highlights

- **Stable release notes unblocked** — Added the required stable release notes file so the Release workflow can publish the `2026.422.0` stable line.

## Fixes

- **Issue comment delta pagination** — Fixed `GET /api/issues/:id/comments?after=...&order=asc` returning 500 by binding cursor anchor timestamps as ISO strings (postgres.js bind compatibility).

